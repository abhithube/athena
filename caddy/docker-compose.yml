services:
  caddy:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: caddy
    restart: unless-stopped
    networks:
      - athena
    ports:
      - 80:80
      - 443:443
      - 443:443/udp
    volumes:
      - ./data:/data
      - ./conf:/etc/caddy
      - ./config:/config
    env_file:
      - .env

networks:
  athena:
    external: true